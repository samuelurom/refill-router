const spotlightSection = document.querySelector(".spotlight-section");

function renderSpotight() {
  const html = `
    <div class="spotlight-heading">
    <button>refresh</button>
    </div>
  `;
}
